# Исправление импорта Location и дублей застройщиков

## Правило выполнения

- Каждый пункт выполнения отмечается чекбоксом.
- Невыполненный пункт: `[ ]`.
- Выполненный пункт: `[x]`.
- По мере выполнения нужно обновлять этот файл и менять `[ ]` на `[x]`.
- Перед запуском repair-скрипта в режиме `run` сначала обязательно выполнить `preview` и проверить отчёт.

## Контекст

- Поле `Район` на детальной странице берёт значение из `RealEstateObject.primaryLocation`.
- Сейчас подтверждён баг импорта: у части объектов `primaryLocation` выбран из `rajon-okolo`, хотя настоящий район должен приходить из `rajony`.
- Пример: у `Дом «Палашёвский 11»` сейчас primary = `На Патриарших`, но корректный район из `rajony` = `Тверской`.
- `rajon-okolo` не удаляем: эти термины нужны как отдельное поле `Окружение`.
- Дубли застройщиков склеиваем только через ручной alias-конфиг, без автоматического выбора канонического имени.

## Implementation Checklist

### Этап 0. Подтвердить текущее состояние

- [x] Проверить текущее количество объектов в `real_estate_objects`.
- [x] Проверить количество объектов, где `primaryLocation.type = AREA`.
- [x] Проверить количество объектов, где `primaryLocation.type = AREA`, но среди связанных `object_locations` есть `DISTRICT`.
- [x] Зафиксировать пример `Дом «Палашёвский 11»`: текущий primary, все связанные `AREA`, все связанные `DISTRICT`.
- [x] Проверить текущие дубли застройщиков по нормализации `lower(trim(name))`.

Результат проверки:

- `real_estate_objects`: 303 объекта.
- `primaryLocation.type = AREA`: 171 объект.
- `primaryLocation.type = AREA` + есть связанный `DISTRICT`: 171 объект.
- `Дом «Палашёвский 11»`: primary = `AREA: На Патриарших #19`, `AREA = На Патриарших #19, Центр #1747`, `DISTRICT = Тверской #1242`.
- Дубли застройщиков по `lower(trim(name))`: 12 групп (`Coldy/COLDY`, `Element/ELEMENT`, `Forma/FORMA`, `Ingrad/INGRAD`, `Level group/Level Group`, `Stone/STONE`, `Tekta Group/TEKTA GROUP`, `Vesper/VESPER`, `Донстрой/ДОНСТРОЙ`, `Кортрос/КОРТРОС`, `Крост/КРОСТ`, `Пионер/ПИОНЕР`).

### Этап 1. Исправить выбор primary location в mapper

- [x] Найти текущую точку выбора `primaryLocation` в `tools/wp-import/src/mapper.ts`.
- [x] Добавить функцию выбора основной локации: сначала самый конкретный `DISTRICT`, затем fallback на первый `AREA`.
- [x] Убедиться, что `rajon-okolo` продолжает попадать в общий список `locations` как `LocationType.AREA`.
- [x] Убедиться, что `rajony` продолжает попадать в общий список `locations` как `LocationType.DISTRICT`.
- [x] Обновить unit tests mapper для сценария `AREA + DISTRICT`.
- [x] Добавить unit test mapper для сценария только с `AREA`.
- [x] Добавить unit test mapper для кейса, аналогичного `Дом «Палашёвский 11»`.

Результат проверки:

- `pnpm --filter @platforma/wp-import test`: 5 tests passed.

### Этап 2. Добавить ручной alias-конфиг застройщиков

- [x] Создать конфиг alias-групп застройщиков, например `tools/wp-import/developer-aliases.json`.
- [x] Оставить конфиг пустым или с явно утверждёнными группами, без автоматического склеивания спорных имён.
- [x] Добавить загрузку alias-конфига в импортёр.
- [x] Добавить нормализацию имени застройщика для поиска: trim, схлопывание пробелов, case-insensitive comparison.
- [x] При импорте сначала искать каноническое имя через alias-конфиг.
- [x] Если alias не найден, не склеивать спорные варианты автоматически.
- [x] Добавить предупреждения в preview/import report для возможных дублей по нормализованному имени.

Результат проверки:

- Создан `tools/wp-import/developer-aliases.json` с пустым списком `groups`.
- Без alias-группы варианты вроде `Coldy/COLDY` не склеиваются автоматически и попадают в warning `possible_developer_duplicate`.
- С явной alias-группой mapper приводит варианты к `canonicalName`.
- `pnpm --filter @platforma/wp-import test`: 7 tests passed.

### Этап 3. Защитить БД от будущих дублей застройщиков

- [x] Добавить поле нормализованного имени застройщика в Prisma schema.
- [x] Добавить миграцию для заполнения нормализованных имён у существующих застройщиков.
- [x] Добавить уникальность по нормализованному имени после ручного устранения конфликтов.
- [x] Обновить `ensureDeveloper`, чтобы он искал застройщика по alias/normalized name, а не только по точному `name`.
- [x] Убедиться, что `slug` остаётся стабильным для канонического застройщика.

Результат проверки:

- Добавлено поле `Developer.normalizedName`.
- Добавлена миграция `20260510120000_add_developer_normalized_name`.
- Миграция успешно применена к локальной БД через `prisma migrate deploy`.
- Уникальный индекс `developers_normalized_name_key` создан.
- Из 146 застройщиков `normalized_name` заполнен у 122; 24 записи из 12 уже конфликтующих групп оставлены `NULL` до ручного alias/repair.
- `ensureDeveloper` сначала ищет по `normalizedName`, затем по точному `name`; `slug` существующего канонического застройщика сохраняется.
- `pnpm --filter @platforma/wp-import test`: 7 tests passed.
- `pnpm --filter @platforma/api test`: 25 tests passed.

### Этап 4. Сделать repair-скрипт

- [x] Добавить CLI-скрипт repair с режимами `preview` и `run`.
- [x] В `preview` показать объекты, где primary стоит `AREA`, но есть связанный `DISTRICT`.
- [x] В `preview` показать, какой `DISTRICT` станет новым primary для каждого объекта.
- [x] В `preview` показать alias-группы застройщиков и планируемые переносы объектов.
- [x] В `run` обновить `real_estate_objects.primary_location_id`.
- [x] В `run` обновить `object_locations.is_primary`.
- [x] В `run` перенести объекты с дублей застройщиков на канонические записи из alias-конфига.
- [x] В `run` удалить пустые записи застройщиков-дублей после переноса.
- [x] Сделать repair-скрипт идемпотентным: повторный запуск не должен менять уже исправленные данные.
- [x] Добавить npm/pnpm scripts для запуска repair preview/run.

Результат проверки:

- Добавлен `tools/wp-import/src/repair.ts`.
- Добавлены команды `pnpm wp-import:repair:preview` и `pnpm wp-import:repair:run`.
- `repair preview` показывает 171 кандидата `AREA -> DISTRICT`.
- `repair preview` показывает 0 alias-групп застройщиков, потому что `developer-aliases.json` пока пустой.
- `repair run` реализован, но не запускался: запись в БД оставлена для этапа проверки и ручного подтверждения.
- После preview в БД всё ещё 171 объект с `AREA` primary и доступным `DISTRICT`, то есть preview не меняет данные.
- `pnpm --filter @platforma/wp-import test`: 8 tests passed.

### Этап 5. Разделить район и окружение в API

- [x] Проверить shared-типы для `LocationType` и связанных локаций.
- [x] Убедиться, что ответы `/objects`, `/objects/:id`, `/objects/slug/:slug`, `/map/objects` отдают все `locations` с `type`.
- [x] Добавить отдельную фильтрацию справочника `/locations?type=DISTRICT` для районов.
- [x] Добавить отдельную фильтрацию справочника `/locations?type=AREA` для окружения.
- [x] Для фильтра `locationId` оставить поиск по связанным локациям, чтобы район и окружение работали независимо.
- [x] При необходимости добавить отдельный query-параметр для окружения, например `areaId`.

Результат проверки:

- Shared-типы уже содержали `LocationType`, `ObjectLocation.type` и `RealEstateObjectBase.locations`; для `MapObject` добавлены связанные `locations`.
- `/objects`, `/objects/:id`, `/objects/slug/:slug` уже отдавали связанные `locations` с `type`; `/map/objects` теперь тоже отдаёт `locations` с `type`, `isPrimary` и `sortOrder`.
- `/locations?type=DISTRICT` и `/locations?type=AREA` поддерживаются и покрыты тестом.
- `locationId` оставлен обратно совместимым: ищет по `primaryLocationId` и связанным `object_locations`.
- Добавлен `areaId` для независимой фильтрации окружения по связанным `AREA`.
- `pnpm --filter @platforma/shared build`: passed.
- `pnpm --filter @platforma/api test`: 28 tests passed.
- `pnpm --filter @platforma/web build`: passed.

### Этап 6. Обновить детальную страницу объекта

- [x] На детальной странице поле `Район` выводить только из `DISTRICT`.
- [x] Если `primaryLocation` по старым данным всё ещё `AREA`, брать первый связанный `DISTRICT` как fallback для отображения.
- [x] Добавить отдельную строку/блок `Окружение` для связанных `AREA`.
- [x] Для `Дом «Палашёвский 11»` ожидаемое отображение: `Район = Тверской`, `Окружение = На Патриарших, Центр`.
- [x] Проверить, что метро и остальные локации не исчезли из детальной страницы.

Результат проверки:

- В верхнем блоке детальной страницы `Район` теперь берётся только из `DISTRICT`.
- Если `primaryLocation.type = AREA`, используется первый связанный `DISTRICT`.
- В блоке локаций добавлена строка `Окружение` для связанных `AREA`; `CUSTOM`-локации остаются в строке `Дополнительно`.
- Balloon карты объекта теперь тоже показывает район из `DISTRICT`, а не окружение.
- Блок метро не менялся и продолжает выводить `object.metroStations`.
- Для данных `Дом «Палашёвский 11»` с `DISTRICT = Тверской` и `AREA = На Патриарших, Центр` helper-логика даёт ожидаемое отображение.
- `pnpm --filter @platforma/web build`: passed.
- После проверки в браузере выяснилось, что `localhost:5173` отдавал старый Docker image без текущих изменений; выполнено `docker compose up -d --build web`, контейнер `platforma-web-1` теперь отдаёт новый `ObjectDetailPage.tsx`.

### Этап 7. Обновить каталог и карту

- [x] В каталоге заменить общий фильтр `Локация` на фильтр `Район` из `DISTRICT`.
- [x] Добавить отдельный фильтр `Окружение` из `AREA`.
- [x] Убедиться, что карточки каталога показывают район, а не окружение.
- [x] Убедиться, что list view показывает район, а не окружение.
- [x] Убедиться, что map view и balloon показывают район, а не окружение.
- [x] Проверить, что фильтрация по району и окружению может использоваться одновременно.

Результат проверки:

- Каталог загружает районы через `/locations?type=DISTRICT&limit=500`.
- Окружение загружается отдельно через `/locations?type=AREA&limit=500`.
- Query-параметр `locationId` оставлен для района, добавлен `areaId` для окружения.
- Запросы каталога и карты отправляют оба параметра одновременно, поэтому фильтры могут комбинироваться.
- Карточки, list view, список карты, карточка выбранного объекта на карте и balloon берут отображаемое значение через `DISTRICT`; старый `AREA` в `primaryLocation` не используется как район.
- `pnpm --filter @platforma/web build`: passed.

### Этап 8. Обновить админку объекта

- [x] В форме объекта переименовать `Основная локация` в `Основной район`.
- [x] В выпадающем списке `Основной район` показывать только `DISTRICT`.
- [x] Добавить отдельный выбор `Окружение` только из `AREA`.
- [x] При сохранении объединять `primaryLocationId`, выбранные районы и выбранное окружение в `locationIds`.
- [x] Убедиться, что существующие дополнительные связанные локации не теряются при редактировании.
- [x] Убедиться, что в таблице объектов админки отображается район, а не окружение.

Результат проверки:

- Админка объектов загружает районы через `/locations?type=DISTRICT&limit=500`.
- Окружение загружается отдельно через `/locations?type=AREA&limit=500`.
- Поле `Основной район` показывает только `DISTRICT`.
- Мультивыбор `Районы` показывает только `DISTRICT`, мультивыбор `Окружение` показывает только `AREA`.
- При сохранении payload собирает `locationIds` из `primaryLocationId`, выбранных районов, выбранного окружения и сохранённых скрытых дополнительных локаций.
- Для старых данных с `primaryLocation.type = AREA` форма выбирает первый связанный `DISTRICT` как основной район, а старый `AREA` остаётся в окружении.
- Таблица объектов админки показывает район через `DISTRICT`, а не старый `primaryLocation`-`AREA`.
- `pnpm --filter @platforma/web build`: passed.

### Этап 9. Проверить импорт и repair

- [ ] Запустить unit tests для `tools/wp-import`.
- [ ] Запустить backend tests.
- [ ] Запустить frontend build.
- [ ] Запустить repair `preview`.
- [ ] Проверить, что preview показывает ожидаемые 171 объекта с `AREA` primary и доступным `DISTRICT`.
- [ ] Проверить preview alias-групп застройщиков.
- [ ] После проверки запустить repair `run`.
- [ ] Проверить, что после repair количество объектов осталось 303.
- [ ] Проверить, что после repair объектов с `AREA` primary и доступным `DISTRICT` стало 0.
- [ ] Проверить, что повторный repair `run` не создаёт дополнительных изменений.

### Этап 10. Manual QA

- [ ] Открыть `Дом «Палашёвский 11»` и проверить `Район = Тверской`.
- [ ] На этом же объекте проверить `Окружение = На Патриарших, Центр`.
- [ ] Проверить несколько объектов, где раньше primary был `AREA`.
- [ ] Проверить фильтр каталога по району.
- [ ] Проверить фильтр каталога по окружению.
- [ ] Проверить фильтры на карте.
- [ ] Проверить форму редактирования объекта в админке.
- [ ] Проверить список застройщиков после утверждённого склеивания дублей.
- [ ] Проверить повторный WordPress import preview: новые дубли застройщиков не должны появляться без предупреждения.

## Assumptions

- Канонические имена застройщиков выбираются вручную через alias-конфиг.
- Новые библиотеки не добавляем.
- `rajon-okolo` остаётся полезной классификацией, но больше не используется как основной район.
