# План реализации закрытой платформы недвижимости

## Правило ведения плана

- Каждый пункт выполнения отмечается чекбоксом.
- Невыполненный пункт: `[ ]`.
- Выполненный пункт: `[x]`.
- По мере выполнения проекта нужно обновлять этот файл и менять `[ ]` на `[x]`.
- Перед крупными изменениями нужно кратко подтвердить следующий блок работ с пользователем.

## Summary

Файл: `plan.md`.

Формат выполнения: каждый пункт отмечается чекбоксом `[ ]`; после выполнения меняется на `[x]`.

Стратегия:

- начинать с аудита WordPress-базы;
- использовать `React + Vite`, `NestJS`, `PostgreSQL + PostGIS`, `Prisma`, `Redis + BullMQ`;
- для файлов на старте использовать `Local + MinIO`;
- не добавлять зависимости вне утверждённого стека без отдельного согласования;
- после создания `plan.md` остановиться и спросить пользователя, можно ли стартовать выполнение.

## Key Changes / Public Interfaces

- Monorepo: `apps/web`, `apps/api`, `packages/shared`, `tools/wp-import`.
- API: `/auth`, `/objects`, `/developers`, `/locations`, `/metro`, `/map`, `/users`, `/files`, `/wordpress-import`, `/audit-log`.
- Frontend routes: `/login`, `/catalog`, `/catalog/map`, `/objects/:slug`, `/cabinet`, `/admin/*`.
- Prisma entities: `User`, `Role`, `Permission`, `RealEstateObject`, `Developer`, `Location`, `MetroStation`, `File`, `ObjectImage`, `ObjectFile`, `AuditLog`, `ImportReport`.
- Env files: `.env.example` для root, `apps/api`, `apps/web`, `tools/wp-import`.
- Scripts: `dev:web`, `dev:api`, `db:migrate`, `db:seed`, `wp-import:preview`, `wp-import:run`.

## Implementation Checklist

### Этап 0. Зафиксировать план

- [x] Создать `plan.md` в корне проекта.
- [x] Перенести в `plan.md` этот чеклист.
- [x] Добавить правило: выполненные пункты отмечаются как `[x]`.
- [x] После сохранения `plan.md` остановиться и спросить пользователя, можно ли стартовать выполнение.

### Этап 1. Аудит WordPress

- [x] Получить read-only доступ к MySQL/MariaDB WordPress.
- [x] Определить реальный `post_type` объектов недвижимости.
- [x] Собрать список `meta_key` из `wp_postmeta`.
- [x] Определить, используются ли ACF-поля и как они связаны с объектами.
- [x] Найти источники названия, slug, описания, статуса, цены, срока сдачи, адреса и координат.
- [x] Найти структуру застройщиков, районов/локаций и метро.
- [x] Найти структуру featured image, gallery, presentation и attachments.
- [x] Подготовить `mapping.md` с маппингом WordPress -> новая БД.
- [x] Зафиксировать риски импорта: отсутствующие координаты, битые файлы, неизвестные поля.

### Этап 2. Каркас проекта

- [x] Создать `pnpm` monorepo.
- [x] Создать `apps/web` на `React + TypeScript + Vite`.
- [x] Создать `apps/api` на `NestJS + TypeScript`.
- [x] Создать `packages/shared` для общих типов.
- [x] Создать `tools/wp-import`.
- [x] Настроить `Docker Compose` для `postgres`, `redis`, `minio`, `api`, `web`.
- [x] Подключить `Prisma` к `PostgreSQL`.
- [x] Включить расширение `PostGIS`.
- [x] Добавить `.env.example`.
- [x] Добавить healthcheck endpoint backend.

### Этап 3. База данных

- [x] Описать Prisma schema для пользователей, ролей и прав.
- [x] Описать Prisma schema для объектов недвижимости.
- [x] Описать справочники `Developer`, `Location`, `MetroStation`.
- [x] Описать связи с метро, изображениями и файлами.
- [x] Описать `AuditLog` и `ImportReport`.
- [x] Добавить технические поля импорта: `wpPostId`, `wpAttachmentId`, `wpTermId`.
- [x] Создать первую миграцию.
- [x] Добавить seed ролей `admin`, `editor`, `user`.
- [x] Добавить seed permissions.
- [x] Добавить seed первого admin-пользователя.

### Этап 4. Auth и RBAC

- [x] Реализовать `POST /auth/login`.
- [x] Реализовать `POST /auth/logout`.
- [x] Реализовать `POST /auth/refresh`.
- [x] Реализовать `GET /auth/me`.
- [x] Хешировать пароли через `Argon2id`.
- [x] Хранить refresh token в `httpOnly cookie`.
- [x] Не хранить токены в `localStorage`.
- [x] Добавить guards для JWT и permissions.
- [x] Закрыть защищённые backend endpoints.
- [x] Добавить frontend auth provider.
- [x] Добавить `/login`.
- [x] Добавить protected routes.

### Этап 5. Пользователи

- [x] Реализовать API списка пользователей.
- [x] Реализовать создание пользователя.
- [x] Реализовать редактирование пользователя.
- [x] Реализовать смену роли.
- [x] Реализовать смену статуса `active`, `blocked`, `invited`.
- [x] Реализовать удаление или архивирование пользователя.
- [x] Добавить админскую страницу пользователей.
- [x] Логировать admin-действия в `AuditLog`.

### Этап 6. Объекты недвижимости

- [x] Реализовать `GET /objects` с пагинацией и фильтрами.
- [x] Реализовать `GET /objects/:id`.
- [x] Реализовать `GET /objects/slug/:slug`.
- [x] Реализовать `POST /objects`.
- [x] Реализовать `PATCH /objects/:id`.
- [x] Реализовать публикацию объекта.
- [x] Зафиксировать, что архивация объекта не нужна: упоминание архива относилось к файлу темы WordPress.
- [x] Реализовать связи с застройщиком, локацией и метро.
- [x] Валидировать координаты, цены, сроки сдачи и обязательные поля.
- [x] Логировать создание, изменение и публикацию.

### Этап 7. Файлы и медиа

- [x] Настроить MinIO для локального S3-compatible storage.
- [x] Реализовать `POST /files/upload`.
- [x] Реализовать `GET /files/:id`.
- [x] Реализовать `DELETE /files/:id`.
- [x] Добавить ограничения MIME type и размера файлов.
- [x] Реализовать загрузку обложки.
- [x] Реализовать загрузку галереи.
- [x] Реализовать сортировку галереи.
- [x] Реализовать загрузку презентации, планировок и документов.

### Этап 8. Админка объектов

- [x] Добавить `/admin/objects`.
- [x] Добавить `/admin/objects/new`.
- [x] Добавить `/admin/objects/:id/edit`.
- [x] Реализовать таблицу объектов с поиском, сортировкой и фильтром статуса.
- [x] Реализовать форму объекта с валидацией.
- [x] Реализовать загрузку файлов в форме.
- [x] Реализовать drag-and-drop сортировку изображений.
- [x] Реализовать предпросмотр карточки.
- [x] Реализовать публикацию из админки.

### Этап 9. Импорт WordPress

- [x] Реализовать MySQL client только для чтения WordPress-базы.
- [x] Реализовать `wp-import:preview`.
- [x] Сформировать отчёт preview без записи в новую БД.
- [x] Реализовать mapper объектов.
- [x] Реализовать mapper застройщиков, локаций и метро.
- [x] Реализовать mapper изображений и презентаций.
- [x] Реализовать `wp-import:run`.
- [x] Сделать импорт идемпотентным по `wpPostId`, `slug`, `wpAttachmentId`.
- [x] Сохранять `ImportReport`.
- [x] Добавить `/admin/import`.
- [x] Добавить просмотр import reports.

### Этап 10. Каталог

- [x] Добавить `/catalog`.
- [x] Реализовать карточку объекта.
- [x] Подключить список объектов к API.
- [ ] Хранить фильтры в URL query params.
- [ ] Реализовать фильтры: поиск, застройщик, локация, метро, срок сдачи, цена, статус, презентация, координаты.
- [x] Реализовать pagination.
- [x] Добавить loading, error и empty states.
- [ ] Добавить переход на карту каталога.

### Этап 11. Детальная страница объекта

- [ ] Добавить `/objects/:slug`.
- [ ] Вывести основные данные объекта.
- [ ] Вывести описание и особенности.
- [ ] Вывести галерею.
- [ ] Вывести презентацию и файлы.
- [ ] Вывести метро и локацию.
- [ ] Добавить fallback для отсутствующих данных.
- [ ] Добавить возврат к каталогу.

### Этап 12. Яндекс.Карта

- [ ] Подключить Yandex Maps JS API лениво.
- [ ] Реализовать общий map component.
- [ ] Добавить карту одного объекта на странице объекта.
- [ ] Добавить fallback, если координат нет.
- [ ] Добавить `/catalog/map`.
- [ ] Реализовать `GET /map/objects`.
- [ ] Вывести markers объектов.
- [ ] Добавить popup/card при клике на marker.
- [ ] Добавить переход с marker на страницу объекта.
- [ ] Добавить кластеризацию при большом количестве объектов.

### Этап 13. Личный кабинет

- [ ] Добавить `/cabinet`.
- [ ] Вывести профиль пользователя.
- [ ] Вывести роль и статус.
- [ ] Показать доступные разделы по permissions.
- [ ] Скрыть недоступные разделы на frontend.
- [ ] Проверить, что backend всё равно защищает доступ.

### Этап 14. Стабилизация

- [ ] Проверить роли `admin`, `editor`, `user`.
- [ ] Проверить все auth-сценарии.
- [ ] Проверить CRUD пользователей.
- [ ] Проверить CRUD объектов.
- [ ] Проверить импорт preview.
- [ ] Проверить импорт commit.
- [ ] Проверить повторный импорт без дублей.
- [ ] Проверить каталог и фильтры.
- [ ] Проверить карту каталога и карту объекта.
- [ ] Проверить загрузку файлов.
- [ ] Проверить audit log.
- [ ] Проверить адаптивность основных страниц.
- [ ] Подготовить staging/production env checklist.

## Test Plan

- [ ] Unit tests для mappers WordPress import.
- [ ] Unit tests для permissions и guards.
- [ ] Unit tests для сервисов объектов и пользователей.
- [ ] API tests для `/auth`, `/objects`, `/users`, `/wordpress-import`.
- [ ] E2E smoke test: login -> admin -> create object -> publish -> catalog -> object details.
- [ ] E2E smoke test: import preview -> import run -> повторный import run без дублей.
- [ ] Manual QA: роли, карта, загрузка файлов, фильтры, empty/error states.
- [ ] Проверить, что обычный `user` не имеет доступа к admin endpoints.
- [ ] Проверить, что `editor` не может управлять пользователями и запускать импорт.
- [ ] Проверить, что отсутствующие координаты не ломают каталог и страницу объекта.

## Assumptions

- На первом этапе не реализуются SEO, sitemap, OpenGraph, 301 redirects, CRM, обучение, курсы, тесты, платежи и сложная аналитика.
- Storage по умолчанию: `MinIO` локально как S3-compatible слой.
- Практический порядок: сначала WordPress audit, затем каркас проекта.
- Чекбоксы должны обновляться прямо в `plan.md` по мере выполнения.
- Любые новые зависимости сверх утверждённого стека добавляются только после отдельного согласования.
