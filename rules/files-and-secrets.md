# Генерируемые и чувствительные файлы

- Не редактируй вручную `dist`, `node_modules`, `*.tsbuildinfo` и lockfile без необходимости.
- Не раскрывай и не коммить секреты из `.env`.
- Используй `.env.example` как источник ожидаемых переменных окружения.
