# SESSION NOTES

Общий журнал между чатами. После каждой законченной задачи агент
дописывает сюда краткое саммари: что сделано и что важно помнить.
Без полного расписывания — 2–4 строки на задачу.

Новые записи — вверху (обратный хронологический порядок).
Кратко и по фактам, без воды. Хронология и отчёты — не требования,
это рабочая память между сессиями.

---

## 2026-09-17

### Справочник маршрутов до метро: полный набор из OSM и пакетный расчёт

- Входы метро теперь берутся из OpenStreetMap: `pnpm assistant:metro:osm -- --out <geojson>` (`apps/api/scripts/assistant-metro-osm-geojson.cjs`, Overpass, bbox Москвы с краем области). Вход `railway=subway_entrance` привязывается к станции через `public_transport=stop_area` (ближайший узел станции; для МЦК — имя relation), 4 входа-сироты — к станции ≤300 м, 4 станции без входов (Звенигородская, Чеховская, Крестьянская Застава, Сретенский бульвар) представлены узлом станции. Снимок закоммичен: `apps/api/assets/assistant/metro-access-points.osm-20260917T113649Z.geojson` — 1278 точек, 260 станций (было 273 точки/239 станций из тайлов OpenFreeMap). Версия набора `osm-20260917T113649Z-metro-entrances`; на проде импортировать тот же файл (та же версия с другим содержимым падает `ASSISTANT_METRO_DATASET_VERSION_CONFLICT`).
- Роутер: ключ OpenRouteService на бесплатном плане даёт 50 матричных запросов в сутки (`x-ratelimit-limit: 50`, сброс ~14:36 МСК), а пообъектный расчёт стоил 1 запрос на объект. Добавлен `MapRoutingService.warmWalkingRoutes`: недостающие/устаревшие пары origin→destination считаются многоисточниковыми матрицами (≤2500 маршрутов на запрос, `OPENROUTESERVICE_MATRIX_MAX_ROUTES`), в кэш `map_walking_route_cache` пишутся только запрошенные пары. `refreshPublishedObjects` сначала прогревает все опубликованные объекты без актуального факта (один SQL с LATERAL по трём ближайшим точкам), затем штатный проход идёт из кэша. Итог на dev: 314/314 фактов, 933 пары, 11 запросов к ORS, 16 с; медиана 9 мин до метро, 165 объектов ≤10 мин.
- Запуск: `OPENROUTESERVICE_API_KEY=… pnpm assistant:metro:refresh -- --file apps/api/assets/assistant/metro-access-points.osm-20260917T113649Z.geojson --version osm-20260917T113649Z-metro-entrances` (скрипт читает `apps/api/.env`, где ключа ORS нет — он в корневом `.env`). Смена набора делает все факты устаревшими, полный пересчёт — ~11 запросов.
- Планировщик: «не дальше 7 минут пешком от метро» давал застройщика «метро», а «до метро в Хамовниках» — фильтр метро «в Хамовниках» (оба из детерминированных извлекателей, потому считались заземлёнными) → пустая выдача при живых фактах. Извлекатели метро/застройщика получают текст без «до/от/к/у метро» без названия станции; «у метро Курская» и «до метро Сокол от застройщика ПИК» работают как раньше.
- Проверки: api 978/978 (новые тесты map-routing warm-up и t02), харнесс t05 на одноразовой БД — 3 metro-теста включая новый прогрев с реальным SQL. Живые запросы на qwen-flash: «не дальше 7 минут пешком от метро» — 3188 лотов, карточки с фактом «2 мин пешком до метро «Тушинская»»; «до метро в Хамовниках» — 639 лотов (Фрунзенская набережная); «метро Соколиная Гора» (МЦК) резолвится из справочника.
- Выкачено на прод как `metro-20260917T1209Z` (`50a7307`): инкрементальный бандл поверх `card-…`, пересоздан только api (web остался на `bdd9886`), миграций нет, бэкап и `deploy-result.txt` в `/opt/platforma-deploy-backups/metro-20260917T1209Z/`. Справочник метро на проде был пустой; `assistant-metro-refresh` внутри контейнера api загрузил 1278 точек и посчитал маршруты для 339/339 объектов (1014 пар, 12 матричных запросов, 19 с). Ключ ORS на проде тот же, что локально — суточная квота 50 общая.
- Прод-ассистент переведён в живой режим по команде руководителя (тот же образ, только env): в `/opt/platforma/.env.production` добавлены `ASSISTANT_AI_MODE=alibaba`, `ASSISTANT_QUERY_PLANNER_LIVE`, `ASSISTANT_PAID_CALLS_CONFIRMED`, бюджет `ASSISTANT_MODEL_DAILY_BUDGET_USD=5.00`, лимиты запросов, `ASSISTANT_ALIBABA_BASE_URL` (dashscope-intl), эмбеддинги `text-embedding-v4/256`, `ASSISTANT_GEO_PROVIDER_MODE=locationiq` + бюджеты, Overpass, `ASSISTANT_EXTERNAL_CONNECTORS_ENABLED`, `ASSISTANT_CURRENT_FACT_REFRESH_MODE=live`, `ASSISTANT_RELEASE_SHA`, ключи Alibaba и LocationIQ (те же, что локально; ключи дописывал руководитель сам — автоматика не даёт агенту копировать секреты). Rollout по-прежнему `ADMINS`. Важно: в production (`DEPLOYMENT_ENV=production`) живые эмбеддинги требуют `ASSISTANT_EMBEDDING_BENCHMARK_WINNER=<model>:<dims>:<sha256 датасета бенчмарка>` (константа в `assistant-embedding.gateway.ts`), без неё api падает на старте `ASSISTANT_EMBEDDING_BENCHMARK_WINNER_REQUIRED` — первый рестарт так и упал (~2 мин простоя api), строка добавлена. Откат: `env.production.before-live` в бэкапе метро-релиза.
- Открыто: на проде нет источников базы знаний и воркер источников выключен в prod-compose (`ASSISTANT_SOURCE_WORKER_ENABLED: "false"`), так что вопросы «условия рассрочки в ЖК X» на проде ответят «источник не подключён»; локальный кэш ориентиров «метро X» старой версии набора живёт 180 дней.

## 2026-09-17

### Карточка ЖК: вариант C «Кинозал» (утверждён руководителем)

- Требование: первое фото 16:9 и сильно крупнее, вся информация под ним. Макеты A/B/C — артефакт «Новая карточка ЖК», выбран C. В `ObjectDetail` (`apps/web/src/objects/ObjectDetailPage.tsx`) убраны шапка и правая колонка: галерея 16:9 на всю ширину (разделы, счётчик и «Во весь экран» поверх фото сверху, лента миниатюр стеклом снизу, свайп на телефоне), под ней липкий паспорт `.object-passport` (название, район и метро с цветом линии, цена + ₽/м² + число лотов, Презентация/Аэротур/Планировки, «Подобрать лот», карандаш), затем «Характеристики» (`getObjectSummaryFacts` + «Лотов в продаже») и «Документы» (доп. файлы). Остальная страница без изменений.
- Телефон (до 760px): паспорт не липнет, «Подобрать лот» переезжает в док `.object-mobile-dock` внизу (цена «от 23,7 млн ₽»); когда есть кнопка ассистента, док поднят на 94px. Внешний вид — в `fluffy-white-theme.css` (токены `--fw-passport-bg`, `--fw-hero-shadow`, `--fw-dock-*` для обеих тем), раскладка — в `styles.css`. Страница лота осталась на `.object-detail-hero`; мёртвые правила `.object-detail-hero .object-files-section*` и `.object-detail-header-actions` удалены.
- Выкачено на прод как `card-20260917T0944Z` (`bdd9886`): инкрементальный бандл поверх релиза `links-…`, пересоздан только web (api остался на `3645ee6`), бэкап и `deploy-result.txt` в `/opt/platforma-deploy-backups/card-20260917T0944Z/`.

### Подборка ЖК: ссылки последней страницы из каталога FluffyWhite

- `PROJECT_PRESENTATION_LINKS` (`packages/shared/src/project-presentation-template.mjs`) теперь хранит ссылки с 14-й страницы `~/Downloads/Catalog_feb_2026.pdf`: Instagram `fluffywhite.estate`, Telegram-канал `t.me/+OacAOVxTqWM0Y2Ji`, YouTube `@fluffywhite.moscow`, «Начать подбор» `clck.ru/3QmQoS` (ведёт в `t.me/fluffywhite` с заготовленным текстом). Заглушек-`span` больше нет, все три плашки — ссылки.
- Выкачено на прод как `links-20260917T0751Z` (`3645ee6`): пересозданы только api и web, миграций нет, бэкап и `deploy-result.txt` в `/opt/platforma-deploy-backups/links-20260917T0751Z/`.
- «Узнать подробности» на страницах ЖК по-прежнему ведёт на `snapshot.cta.url` (`t.me/FluffyWhite`), в превью — `PROJECT_PRESENTATION_LINKS.chat`. Снапшот и его версия не менялись: ссылки финальной страницы берутся из шаблона в момент печати.

## 2026-09-17

### Ассистент переведён в живой режим на Alibaba и починен под Qwen

- Аккаунт Alibaba был под риск-контролем (`RISK.RISK_CONTROL_REJECTION` → API 403 `AccessDenied.Unpurchased`), снято после верификации. Ключ в `.env` и `apps/api/.env`, `ASSISTANT_AI_MODE`/`ASSISTANT_EMBEDDING_MODE=alibaba`, адрес `dashscope-intl` (workspace-адреса `ws-*.maas.aliyuncs.com` не нужны). Модели: Luna=`qwen-flash` ($0.05/$0.40 за 1M), Terra=`qwen3.8-max` ($2/$6), каталог цен `alibaba-dashscope-pricing-2026-09-17`. В запрос добавлен `enable_thinking:false` (qwen-plus со strict json_schema уходил в цикл табов до 2500 токенов и 45 с), в промпт — слово JSON (DashScope требует его для qwen-max/json_object). Прод-compose по-прежнему без `ASSISTANT_AI_MODE`.
- Планировщик: план больше не отвергается целиком за незаземлённые значения. `sanitizeLogicalPlanAgainstRequest` выбрасывает незаземлённые фильтры, предикаты, цели сравнения и уточнения и подставляет детерминированные; обязательные факты SEARCH/COMPARE объединяются на сервере. Qwen выдумывал `rooms:[1,2,3]`, `objectType` и INSIDE-предикаты в 100% запросов, а Terra-fallback (~9 с) не влезал в дедлайн 15 с.
- Гео: «метро X» резолвится из `assistant_metro_access_points` (239 станций) без LocationIQ и сохраняется как verified-ориентир `metro_directory` на 180 дней (LocationIQ на «метро X» отдаёт автобусные остановки → Overpass → UNAVAILABLE).
- Район: извлекатель понимает предложный падеж («в Хамовниках», «в Марьине»), канонизация через `locations`/tsquery уже была. Раньше такой запрос шёл без района и отдавал МЫС из Ясенево как точное совпадение.
- Названия ЖК: новый `assistant-object-identity.ts` — сопоставление через латинский slug (Слава↔SLAVA, Set↔СЕТ, Nicole↔Николь) в каталоге, SQL сравнения, ранжировании и разрешении проекта базы знаний. Регулярка целей сравнения знает «по ипотеке/рассрочке/акциям».
- Воркер источников: убран `unref()` таймера (процесс завершался после первого тика). Переиндексация на `text-embedding-v4` выполнена штатно: ingestion теперь сам переобрабатывает неизменившуюся ревизию, если модель активных чанков не совпадает с текущей (`SOURCE_EMBEDDING_MODEL_CHANGED`), переобработка идемпотентна (факты с пропуском дублей и переактивацией, чанки апсертом по ordinal). Запуск — `POST /assistant/sources/:id/refresh` по всем активным источникам. DashScope принимает не более 10 строк на запрос эмбеддингов, батч в ingestion снижен с 64 до 10 (иначе `ASSISTANT_EMBEDDING_PROVIDER_REJECTED`). Итог: 97/97 активных чанков на `text-embedding-v4/256`.
- Проверки: api 975/975, web 352/352, харнессы t01 и fix-token зелёные на одноразовой БД, t06 падает на пустой базе (известное). Живые запросы на qwen-flash за 4–9 с: поиск с районом, сравнение SLAVA/СЕТ (167/715 лотов), факты по Nicole/Slava, «рядом с метро Фрунзенская». Открыто: «рядом с ЖК X» уходит в LocationIQ, а не в каталог; маршрутные факты до метро 50/314; виджет не рисует CLARIFICATION/UNAVAILABLE отдельно.

## 2026-09-16

### Production deploy full-20260916T2115Z (main 96807a5)

- На прод (`193.168.48.214`) выкачен весь `main` вместо релиза карты `fcac82a`: git-бандл → `/opt/platforma-releases/full-20260916T2115Z-96807a5…`, бэкап (`.env`, pg_dump + sha256 + list) в `/opt/platforma-deploy-backups/full-20260916T2115Z/`, образы `release-96807a5…-20260916T2115Z`, итог в `deploy-result.txt` там же. Compose: `docker compose --env-file /opt/platforma/.env.production --project-directory <release> -f docker-compose.yml -f docker-compose.production.yml`.
- Миграции ассистента требуют pgvector: Postgres пересоздан на `platforma-postgres:16-postgis3.5-pgvector0.8.6` (том `platforma_postgres_data` сохранён, данные сверены), применено 16 миграций (итого 76). Пересозданы postgres, api, web; redis, minio и `training-voice-worker` (patch5-образ, код обучения не менялся) не трогались.
- ИИ-помощник на проде: `ASSISTANT_MODULE_ENABLED=true`, `ASSISTANT_ROLLOUT_STAGE=ADMINS`, `ASSISTANT_AI_MODE=fake` — иконку видит только роль admin (6 активных), user/training_pilot — нет (проверено `isAssistantEnabledForActor` по реальным правам). GitHub `origin/main` по-прежнему на `d8ced67` — не пушили.

### Аудит AI-ассистента: что нужно для живого режима (исследование, без изменений кода)

- Код спеки `docs/helpar/speka.md` реализован целиком (t01–t07, fix-*, zaebal1–6, fix-geo2), `pnpm test` api 966/966, web 352/352, `tsc` чистый. Но везде работает fake-планировщик: `ALIBABA_API_KEY` пуст и в `.env`, и в api-контейнере; `ASSISTANT_AI_MODE`/`ASSISTANT_EMBEDDING_MODE=fake`; прод-compose не передаёт `ASSISTANT_AI_MODE`, `_QUERY_PLANNER_LIVE`, `_PAID_CALLS_CONFIRMED`, `_MODEL_*`, `_ROLLOUT_STAGE`, `_GEO_*` и ни одна assistant-переменная там не `:?required`. Fake-планировщик, в отличие от fake-гео/эмбеддингов, в production не запрещён.
- Живой стек: контейнер `assistant-source-worker` 12 дней на образе от 31.08 (до миграции на Alibaba) с `ASSISTANT_EMBEDDING_MODE=openai`/`text-embedding-3-small`, тики падают, причина в логах не пишется (`assistant-source.worker.ts:83` глотает ошибку). Чанки: 530 с `text-embedding-3-small/256` + 151 hash-64 — при переходе на `text-embedding-v4` переэмбеддить всё (retrieval фильтрует по точному model/dims). 3 источника из 20 в `ASSISTANT_EMBEDDING_PROVIDER_REJECTED`, 27 `SOURCE_FETCH_TIMEOUT`. `GET /assistant/eval/runtime` отдаёт 500: `ASSISTANT_RELEASE_SHA` пуст.
- Данные: метро-справочник 273 точки (`openfreemap-20260830`), маршрутные факты у 50/314 опубликованных объектов — `assistant:metro:refresh --file` с полным покрытием не выполнен, GeoJSON-фикстуры в репо нет. Ориентиров 4, из них 2 fake-провайдера со статусом verified («Павелецкая Плаза», мусорная «воды примерно до 30 миллионов») — чистить перед live. Алиасов 0.
- Незакрытые узлы кода: discovery без web search (промпты требуют его, DashScope `enable_search` не портирован, гейт `ASSISTANT_SOURCE_DISCOVERY_WEB_SEARCH_UNVERIFIED`); прод-воркер под profile `assistant-external` и без `ASSISTANT_EXTERNAL_CONNECTORS_ENABLED` — crash-loop; в админке нет создания источников (только POST/CLI); виджет рендерит `CLARIFICATION/UNAVAILABLE/REFUSAL/SAFE_BOUNDARY` голым текстом, показывает сырые коды ошибок, прогресс одной строкой; у `embedding` нет ANN-индекса. Rollout: ADMINS, для PILOT нужен зелёный 200-case eval именно на alibaba, когорта 8–12 не-админов, источники без ошибок <36 ч.

### Презентации ЖК (подборка): новый шаблон PDF по рефу FluffyWhite «ТОП 12 ЖК у парков»

- Реф — `~/Downloads/FluffyWhite-Top12-ZhK-u-parkov_4.pdf` (540×720 pt = страница 720×960 px). Шаблон один на PDF и превью: `packages/shared/src/project-presentation-template.mjs` (+ `.d.mts`), значения сняты из PDF через PyMuPDF (координаты, кегли, цвета, радиусы), расхождение с рефом — ≤1 px. Шрифты: FW Display = Involve (Regular/Medium), FW Text = Inter 4.1, акценты — Lora Italic; все OFL, лежат в `packages/shared/assets/project-presentation-fonts`. В рефе кернинг выключен — `font-kerning:none!important` (шорткат `font:` его сбрасывает).
- Генерация: pdfkit → Chromium (playwright-core, `page.pdf`). Карта: MapLibre 6 + OpenFreeMap в том же браузере (`project-presentations-map.ts`, ES-модули отдаются с виртуального `https://map.invalid`), скриншот + пиксельные позиции маркеров; без копирайта OSM (решение пользователя). Если карта не отрисовалась — маркеры на нейтральном фоне, PDF всё равно собирается. В Alpine-Chromium нет SwiftShader: в образ API добавлены `mesa-egl mesa-gles mesa-dri-gallium`, флаги `--use-gl=angle --use-angle=gl-egl` (только linux). Env: `PROJECT_PRESENTATIONS_BROWSER_EXECUTABLE_PATH`, `PROJECT_PRESENTATIONS_MAP_ENABLED` (в тестах `false`), `PROJECT_PRESENTATIONS_MAP_STYLE_URL`. Время на 3 ЖК в докере ≈16 с (карта ≈13 с), первый прогон холодный ≈70 с.
- Данные: миграция `20260916180000_add_project_presentation_map_title` (поле «Заголовок страницы с картой» на шаге 3). Снапшот v2 (`map.title`), v1 рендерится с дефолтным заголовком. Телефон — брокера, который запускает генерацию (раньше брался автор черновика), российские номера форматируются. Перед генерацией (веб + сервер) обязательны: имя клиента, заголовок карты, у каждого ЖК 4 преимущества (≤40), ровно 3 фото из галереи (автоподстановки больше нет), описание ≤430 (текст из карточки обрезается по слову). «Продолжить» и переходы по шагам не пускают дальше первого незаполненного шага.
- Карта рисуется за один проход: стиль скачивается и локализуется до создания карты, камера сразу на объектах (≈6 с вместо ≈8). Таймаут 20 с на этап, при зависании вторая попытка на чистой странице (`PROJECT_PRESENTATIONS_MAP_TIMEOUT_MS`, `PROJECT_PRESENTATIONS_MAP_ATTEMPTS`), пока рисуется — прогресс тикает 60→72 раз в 2 с. Лог: `Project presentation printed: … map Xs / fallback after Xs`. «Сформировать PDF» заблокирована, пока текущий документ PENDING/RUNNING: на тесте пользователь нажимал трижды при медленной карте (30 с), дубли вставали в очередь и казалось, что PDF «не делается».
- Превью редактора — тот же HTML в iframe (`srcDoc`, масштаб по ширине) + офскрин-снимок карты (`projectPresentationMapSnapshot.ts`). Удалены pdfkit-шаблон, старые React-страницы превью, золотой логотип и Noto Serif Display. В параметрах карточки остались только Стоимость/Класс/Метро (район, девелопер, срок в новом PDF не выводятся, в данных сохраняются). Ссылки соцсетей — в `PROJECT_PRESENTATION_LINKS` (см. 2026-09-17).

### Профиль: экран из рефа, блок «Разделы для роли» удалён

- Страница собрана как `.fw-profile-grid`: шапка `page-header` («Профиль» + подпись + статус справа) и две карточки — `cabinet-profile-card` (аватар 68px, имя 21/32, e-mail, роль справа; форма в две колонки 14px: Имя | Телефон брокера, Почта брокера и «Фото профиля» + «Загрузить фото» на всю ширину, «Сохранить профиль») и `cabinet-security-card` (иконка замка, «Смена пароля» 16.38px + подсказка, три поля, кнопка outline). Радиус карточек 26, padding 24 — как в рефе.
- Удалены блок «Разделы для роли» и его модель: `cabinetSections`, тип `CabinetSection`, `getAvailableCabinetSections`, `canAccessCabinetSection`, `getCabinetSectionPath`, состояние `areSectionsVisible`, стили `.cabinet-section-*`/`.cabinet-badges`. Тесты, которые проверяли маршруты через записи `cabinetSections` (feeds, catalog-links, assistant-audit, presentations), переписаны на роутинг и админ-домашнюю.
- Дубль убран: `details-list` с Email/телефоном/почтой/ролью больше не нужен — e-mail под именем, роль плашкой, контакты брокера в полях формы. Адаптив: ≤1100 — одна колонка, ≤560 — аватар и роль в строке, имя ниже.

### Тёмная тема по рефу Fluffy White (.fw-app.dark) на всю платформу

- Значения сняты с живого рефа: одни и те же элементы до/после переключения темы на 22 экранах (1564 пары) + hover/active. Карта light→dark почти однозначна: панели/контролы `rgb(42 44 46 / 52%)`, текст `#f1f2f3`, muted `#999ca0`, линии `rgb(255 255 255 / 4.7%)`, hover `rgb(255 255 255 / 10%)` + рамка 17%, primary-кнопка `#2b2e31`/белый текст, меню `rgb(20 21 22 / 96%)`, фон — градиент `#070809 → #121314 → #050607`.
- `fluffy-white-theme.css` теперь общий для обеих тем: префикс `:root[data-app-theme] body`, цвета только через `--fw-*`; блок `:root[data-app-theme="dark-premium"]` задаёт тёмные токены. Новые токены: `--fw-primary-bg/-text/-hover-text`, `--fw-solid*` (выбранный чип, «Готово», «Подобрать лот»), `--fw-outline-line`, `--fw-fact-pill-bg` (в тёмной плашки фактов прозрачные, как в рефе), `--fw-menu-bg`, `--fw-option-bg`, `--fw-dialog-bg`, `--fw-card-hover-shadow`, орбы и selection.
- `app-theme.css`: сине-золотая палитра dark-premium заменена токенами рефа (`--app-theme-primary: #96989b` — индикаторы/прогресс, как `--primary` рефа), удалены все золотые правила только для dark-premium; шрифт SF Pro общий. В `styles.css` 155 захардкоженных цветов текста (#18202a, #303b47, #536172, #6d7885, ссылки/ошибки/успех) → `--catalog-ink-*` и семантические токены. Ассистент-чат получил тёмный блок (оранжевые акценты сохранены).
- Проверка: аудит контраста текста с фоном на 26 маршрутах в обеих темах — в тёмной 0 проблем (кроме атрибуции карты), в светлой регрессий нет. Карта (OpenFreeMap liberty) осталась светлой — тёмный стиль карты отдельная задача.

### Карточка лота: вариант A (как страница ЖК)

- Утверждён вариант A (artifact «Карточка лота изнутри»). Порядок: «Вернуться к объекту» → шапка (название; дом · корпус, секция · этаж; «Скачать планировку» = `layout-photo` или первое медиа, аэротур) → `object-detail-hero object-lot-hero` (карусель планировок на белой сцене с лентой миниатюр 112×74 + сводка: «Стоимость»/«Цена со скидкой», статус, цена, ₽/м², плашки Тип/Площадь/Жилая/Кухня/(Потолки/Мощность/Вход)/Этаж/Срок/Корпус — только заполненные, «Добавить в работу» внизу) → «Дом» (обложка, метро, адрес, застройщик, «Открыть объект») | «Ещё в этом доме».
- «Ещё в этом доме»: `feed-units/groups` с тем же `type` и `rooms`, 4 ближайших по цене; «Все N» → `/objects/:slug?lotRooms=N#object-lots`. Блок скрывается, если соседей нет. Площадь лота с десятыми (140,7 м²), срок «1 кв. 2030».
- Баг страницы ЖК: у секции лотов не было `id="object-lots"` — «Подобрать лот» ничего не делал. Добавлен id и прокрутка по хэшу `#object-lots` при открытии. Удалены `.object-lot-split-card`, плашки с точками-выносками и `getObjectLotFactRows`. CTA в FW-теме графитовая, как «Подобрать лот».

## 2026-09-15

### Страница ЖК: вариант A (как экран объекта рефа) + фильтры лотов чипами

- Утверждён вариант A (artifact «Страница ЖК изнутри») с правкой: поиск и фильтры лотов сохранены в новом стиле каталога. Порядок: «Вернуться к каталогу» → шапка (название, район · окружение · м.) → `object-detail-hero` (галерея с видимой лентой миниатюр + сводка: цена `feedPriceFrom ?? priceFrom`, заполненные факты, Презентация/Аэротур/Планировки, доп. файлы, «Подобрать лот» → #object-lots) → «О проекте» (сворачиваемое описание, застройщик → каталог по developerId) | «Расположение» (район, метро, «Показать на карте» → #object-map) → лоты → «Архитектура/Инфраструктура/Наполнение» → карта.
- Лоты: строка поиска (`search` по номеру/названию/адресу — API уже поддерживал) + `FilterPill` (статус, тип | цена, комнаты, этаж, срок сдачи: год + кварталы) — те же параметры запроса, URL-инициализация и сброс. Строки комнатности: название, «площадь · N лотов», диапазон цены, «Показать/Скрыть». Лоты начинаются на 1206px вместо 1329.
- `FilterPill`/`FilterPopoverFooter`/`formatRangeValue` вынесены из CatalogPage в `components/FilterPill.tsx`. Удалены сетка старого тулбара (`.object-feed-units-filter--*`) и `.object-parameters-files-grid`; стили галереи переопределены только внутри `.object-detail-page` (карусель лота не тронута).

### Каталог: карточка ЖК «вариант A» (как .fw-property-card) и список в том же стиле

- Утверждён вариант A (artifact «Карточка ЖК в выдаче»). Карточка: фото 230px, на фото — застройщик и «N лотов по фильтру» стеклянными подписями, индикаторы справа; тело — «Район · м. станции» 11px, название 22/500 (2 строки), цена 20/500, цена за м², две плашки «Срок · …»/«Площадь · …» и «Открыть объект» на всю ширину. Высота 693 → 475px.
- `formatCatalogCardFact`: «71.1-366» → «71,1–366», «Не указан(о)» → «—». Удалены блоки location/metro-dot/facts 2×2/actions и хелперы `normalizeLineColor`, `formatListMetroStations`, `formatListCompletion`.
- Список = та же карточка горизонтально (320px фото + тело + кнопка справа; ≤1100 — фото на две строки, ≤700 — вертикально), три плашки: срок, площадь, застройщик; «Найдено лотов» — подписью на фото.

### Каталог: фильтры «вариант A» — строка чипов + sticky-панель

- Утверждён вариант A из макета (artifact «Компоновка фильтров каталога»): поиск + «Сбросить» (disabled без активных фильтров, на ≤700px — иконка) и одна строка `CatalogFilterPill` — Раздел | Застройщик, Район, Окружение, Метро, Срок сдачи | Цена, Комнаты, Этаж. Меню — общий `Dropdown` (портал); цена/этаж — поповеры с полем «от — до», «Очистить»/«Готово». Кнопка «Скрыть фильтры» и сетка `.catalog-filter-fields` удалены.
- Срок сдачи теперь список лет (текущий−1 … +7, плюс год из URL), а не свободный ввод. Выбранный чип тёмный со значением (метро — только название станции, «+N» для нескольких).
- `.catalog-filters` — `position: sticky; top: 12px; z-index: 30`; `useCatalogStickyPanel` вешает `is-stuck` (доп. тень). На телефоне чипы скроллятся по горизонтали. В `Dropdown.tsx` Tab/Home/End перехватываются только на опциях — поля в поповерах работают нативно.

### Карточка каталога: индикатор PDF — иконка документа из рефа

- Текстовый бейдж «PDF» заменён на SVG-иконку документа из `.fw-property-media button` (`CatalogDocumentIcon` в `CatalogPage.tsx`, пути 1:1 с рефа). Светлая тема: 36×36, radius 11, `rgb(255 255 255 / 28%)` + blur 14, hover `--fw-hover-bg/line` + подъём, active — нажатие (сверено computed-стилями). Тёмная тема — нейтральный круг 36px.
- Локально у первых объектов нет презентаций — проверка шла через подмену ответа `/objects` в Playwright.
- После утверждения по макету (artifact) планировка и аэротур приведены к той же форме: все три индикатора — квадрат 36×36 r11, глиф 18px. Ассеты `floor-plan.svg`/`aerotour-icon.png` рисуются маской в `currentColor` (`getCatalogCardIconMaskStyle`), медные переменные/фильтры удалены. Тёмная тема: подложка карточки 86% + рамка, hover — золотая рамка и глиф.

### Единый дропдаун на всю платформу (эталон — «Сколько комнат» в каталоге)

- Новые `components/Dropdown.tsx` (Radix Popover-портал: z-index 1300, ширина ≥ триггера, max-height 260, стрелки/Home/End/typeahead/Escape, поиск через `matchesSearchVariants`), `SelectDropdown` (замена нативного select, `searchable`, `emptyValue` → приглушённый текст), `AutocompleteInput` (замена datalist). `MultiSelectDropdown` и `CatalogFilterSearchSelect` переведены на портал — меню больше не обрезается overflow (фильтр лотов на странице ЖК, таблицы).
- Нативных `<select>`/`<datalist>` в `src` не осталось (тест `platform-dropdowns`); быстрые редакторы таблицы объектов: выбор = сохранение, Escape/клик мимо = отмена (метро: мимо = сохранить).
- Светлая тема: все меню (портал, инлайн-комбобоксы, сортировка) 11px/400, строки 42px, галочка справа; внутренний input комбобоксов без собственной подложки (обе темы). Браузерные тесты с `selectOption` переписаны на клик по option; stage4 и stage5-part1 падают до дропдаунов (не связано).

## 2026-09-14

### Fluffy White на всю платформу (светлая тема): системный слой вместо точечных правок

- Реф обновился: фон теперь плоский `#ece9e5` + два размытых orb (`#696764` 44%/24%, blur 44px, opacity .65), а не серый градиент из записи ниже. Значения сняты заново через `getComputedStyle` со всех экранов рефа (включая скрытые: профиль, пользователи, объект, история PDF, меню) с `:hover`/`:active`.
- Весь визуальный слой — в новом `apps/web/src/fluffy-white-theme.css` (импорт в `App.tsx` после `app-theme.css`), хвост прошлых overrides из `app-theme.css` удалён. Токены `minimal-luxury` в `app-theme.css` приведены к рефу (ink `#0e0d0c`/`#66635f`, линии `#c3bbb4`/`#a69e96`, статусы, radius-sm/md 9/13). Префикс `:root[data-app-theme="minimal-luxury"] body` (0,2,1) бьёт generic `:is()` и CSS лениво грузящихся модулей.
- Роли: page head 28 / панели 24 / toolbar 22 / property-card 26 / draft-карточки 22 / вложенные блоки 18 (обводка) / факты 10–11 / кнопки 13 / компактные 12 / чипы 9 / бейджи 8. Кнопки: primary светлая → `#383c40` на hover → `#202326` при нажатии; outline/ghost → `rgb(45 38 32/10%)`; везде `translateY(-1px)`/`+1px` через `--fw-lift/--fw-press` (0 при reduced-motion). Цвет текста в рефе не анимируется — короткая «вспышка» при уходе курсора с primary ожидаема.
- Админка-главная «развёрнута» как `.fw-admin-grid` (шапка и карточки на фоне), режим «Списком» тоже. Dark-premium не тронут. Проверки: web tests 339/339, `pnpm build` ок, hover/press сверены численно с рефом, скриншоты 16 роутов + mobile 390px.
- Сайдбар доведён до пикселя по рефу: рейл 78px (было 76), убран базовый `border: 1px` у `aside.sidebar` (сдвигал всё на 1px), в свёрнутом виде `padding: 0` у пунктов (FW-правило `0 12px` его перебивало → «Каталог» распирало до 44px и обрезало справа), аватар 36px. Логотип в рейле оставлен по просьбе пользователя (над бургером, по центру колонки) — осознанное отличие от рефа. Блок пользователя внизу теперь `<button class="sidebar-user">` → `/cabinet` (профиль), со стейтами `.fw-user-mini` (202×62, hover затемнение + lift, press +1px); по фидбэку нижний блок (профиль, тема, выход) переведён в стиль пунктов меню: прозрачные 46px-строки без заливки/рамки, hover затемнение + lift, без «нажатого» вида; у кнопки темы появилась подпись «Темная/Светлая тема»; в рейле — иконки 42×46 по центру, аватар без круга. Для dark-premium добавлены аналогичные правила в конец `app-theme.css`. `.nav-list` сохраняет `overflow-y: auto`, но с `padding-block: 2px`, чтобы hover-лифт не срезался сверху.
- Быстрые ссылки каталога (`.catalog-quick-links`) как `.fw-highlights`: padding `20px 22px` у панели, колонки `0 22px` (крайние без внешнего отступа) → разделители `1px #c3bbb4` только на высоту контента, не от края до края; на ≤1100/≤700px горизонтальные разделители с тем же отступом. Заголовки колонок 12px/400.
- Каталог, поиск и строка результатов как `.fw-catalog-tools` + `.fw-catalog-bar`: кнопки вида переехали из-под быстрых ссылок в строку под поиском (`CatalogResultsBar`: «N объектов в подборке», сегменты Карточки/Список с иконками, «На карте», выпадающая сортировка `CatalogSortSelect`). Панель из 4 кнопок сортировки заменена dropdown-ом с 8 вариантами (новые/старые, дешевле/дороже, за м² ↑↓, сдача раньше/позже) — у рефа 3 пункта, но функциональность сортировки платформы сохранена; URL-параметры `sortBy/sortDirection` те же. Поле поиска с иконкой, «Фильтры» с воронкой (outline, не primary). Тесты `catalog-quick-links-page`/`catalog-lot-filters` обновлены под новую раскладку (явное требование пользователя).
- Известное, не моё: переполнение каталога на mobile (было 53px, стало 35px), обрезание выпадающего меню в секции лотов (`overflow: hidden` в базе, в обеих темах). Оранжевая кнопка ассистента и сам чат не перекрашивались.

### Перенос дизайна Fluffy White: шрифт, фон, подложки/тени, hover, поиск (светлая тема) — точные значения из рефа

- Первая итерация адаптировала фон/тени под уже существующую тёплую палитру платформы — по фидбэку пользователя переделано на буквальные значения из референса (fluffy-white-styleguide.../cabinet), без «отсебятины». Токены вытащены не парсингом минифицированного CSS (там десятки перекрывающих друг друга правил-итераций), а через `getComputedStyle()` на живом рефе в Playwright, включая hover-состояния — единственный надёжный способ узнать, какое правило реально победило в каскаде.
- Шрифт: SF Pro Text раньше был только именем без файла. Скачаны реальные `.otf` (regular 400–500, semibold 600–900) в `apps/web/public/fonts/`, `@font-face` в `styles.css`, применены на весь `html[data-app-theme="minimal-luxury"]` через `--platforma-font-sans` (тёмная тема не тронута).
- Фон: точный градиент рефа `radial-gradient(12% 8%, #67696c47) + radial-gradient(86% 31%, #3a3c3e29) + linear-gradient(135deg, #d5d6d7 → #b8babc → #d0d1d2)` — холодный серый, а не адаптированный тёплый.
- Токены панелей в `--app-theme-*` (minimal-luxury): `surface` → `#faf8f5`, `surface-muted` → `#e3ded8`, `border`/`border-strong` → единый `#c3bbb4`, `shadow-panel` → точный inset-стек из рефа (7 слоёв, плюс внешний `0 20px 56px rgb(70 70 66 / 12%)`), `shadow-card` → `none` (плоские карточки в покое), `shadow-card-hover` → `0 12px 28px rgb(38 40 42 / 17%)`.
- Сайдбар: фон полностью непрозрачный `rgb(250 248 245)` (было 92% альфа), blur `34px saturate(150%)` (было 28/145), border убран (только тень через `--app-theme-shadow-panel`). Пункты меню: `font-weight: 400` и на активном, и на обычном (в рефе активный отличается только цветом, не жирностью — было 550/650).
- Поисковая строка (`.catalog-filter-search input` — ближайший аналог `.fw-global-search-field`, глобального топбара в платформе пока нет): фон `#e3ded8`, бордер `#c3bbb4`, radius 13px; hover/focus — фон `#f0ece7`, бордер `#a69e96`, тень + `translateY(-1px)`. Потребовался `!important`: общее правило для всех `input` использует `:is(...)`, а специфичность `:is()` считается по САМОМУ специфичному аргументу списка (там был двухклассовый селектор) — забивало точечное правило независимо от порядка в файле.
- Hover-лифт (`translateY(-1px)`) добавлен на `.nav-item`, `.secondary-button`/`.admin-button--secondary`/`.catalog-view-toggle`/`.catalog-filter-toggle`.
- Догнано по фидбэку пользователя (кнопки и layout при сворачивании меню):
  - `.primary-button`/`.secondary-button` и их варианты (`.admin-button--primary/secondary`, `.catalog-filter-toggle`, `.catalog-map-button`, `.catalog-card-link`, `.catalog-view-toggle` и т.д.) в minimal-luxury теперь буквально как `.fw-btn-primary`/`.fw-btn-outline`: светлая заливка `#e3ded8` в покое, тёмная `#383c40` + белый текст на hover (инверсия, только для primary), radius 13px, `translateY(-1px)` на hover. Раньше primary был тёмным всегда — осознанно решили инвертировать именно и только в minimal-luxury, dark-premium не тронут.
  - `.workspace` (контентная область) раньше всегда имел `padding-left: rail-width` независимо от состояния сайдбара — при разворачивании сайдбар (238px, `position: fixed`) просто наезжал на контент поверх, ничего не сдвигая (пользователь описал это как «страница сжимается»). Добавлен `.app-shell:has(.sidebar--open) .workspace { padding-left: calc(var(--sidebar-width) + 34px) }` + `transition: padding-left 480ms` синхронно с анимацией самого сайдбара. На мобильном (`max-width: 760px`) оставлено старое поведение — сайдбар там работает как оверлей-дровер поверх контента (есть outside-click-to-close), раздвигать контент там не нужно.
- Проверки: `pnpm test` 339/339, computed-стили сверены с рефом напрямую (включая hover), скриншоты каталога/профиля на localhost:5173, `.workspace` padding-left численно подтверждён (110px свёрнут → 272px развёрнут = ширина сайдбара + отступ).
- Важно: SF Pro — проприетарный шрифт Apple, файлы взяты как есть с публично задеплоенного рефа заказчика, лицензионный статус для продакшн-веба не проверялся.

### Сайдбар Fluffy White: фиксы по ревью + точная сверка с референсом

- По ревью: у icon-кнопок (бургер, тема, аватар) убраны заливки по умолчанию (фон только :hover/:focus-visible), профиль+тема+«Выйти» прибиты к низу панели (нужен `flex: 1` на `.sidebar-content`, иначе `margin-top: auto` не работает), заголовок «Платформа брокеров» — тонкий (400, 14px).
- Точные значения из референса: панель 238px / рейл 76px, radius 28px, padding 18px, brand gap 11px, nav margin-top 34px/gap 7px, кнопки 46px/radius 13px, toggle 36px/radius 11px, transition width .48s cubic-bezier(.22,.75,.18,1).
- ВАЖНО про каскад: generic-правила `html[data-app-theme] :is(.sidebar, .admin-users .page-header, ...)` в app-theme.css имеют специфичность (0,3,1), потому что внутри :is() есть двухклассовые селекторы — одноклассовые overrides для minimal-luxury им проигрывают (из-за этого сайдбар был «квадратным»: radius 7px вместо 28px). Рабочий паттерн: `html[data-app-theme="minimal-luxury"] .app-shell aside.sidebar` (0,3,2).
- Бонус-баг: lightningcss в прод-бандле оставляет только `-webkit-backdrop-filter` у базового `.sidebar`, Chromium его не подхватывал — blur продублирован в winning-override с обоими свойствами.
- Проверки: web tests 339/339, web-контейнер пересобран, computed-стили на :5173 сверены численно (radius 28, bg rgba(250,248,245,.92), blur активен, заголовок влезает без ellipsis).

### Каталог: чипы застройщиков/локаций, карточки-плашки под реф

- `.catalog-quick-links` ("Крупные застройщики"/"Основные локации"/"Старты продаж"): список ссылок был вертикальным full-width списком (`display:grid;gap:4px`, `.text-button{width:100%}`) — выглядел как обычный текстовый список, не как в рефе. Переделано в `flex-wrap` пилюли-чипы (`display:inline-flex`, `border-radius:9px`, фон `var(--catalog-blue-soft)`, hover — `translateY(-1px)` + кольцо), по образцу `.fw-highlights button`. Радиус самой панели поднят до 24px (minimal-luxury).
- `.catalog-card` (плитки объектов): радиус поднят с 7-8px (общий `--catalog-radius-md`, не трогали — используется ещё в 30 местах, включая мелкие теги, где 20px сломал бы вид) до 20px точечным override. Главное — в покое карточка была ВСЕГДА со стеклянной тенью (`--app-theme-shadow-panel` через общий `:is(...)` список, тот же трюк со специфичностью `:is()`, что и в прошлый раз), а должна быть плоской (как `.fw-workflow-card`/`.fw-property-card` в рефе) и получать тень `0 12px 28px rgb(38 40 42/17%)` только на hover вместе с `translateY(-1px)`. Пофиксили `!important` в scoped-правиле для minimal-luxury.
- НЕ делали: специфичные бейджи рефа («Проверено», счётчик фото, сердечко-избранное) — в наших карточках нет таких полей в данных (это бейджи функционально другого типа: план этажа/аэротур/PDF), добавление новых meta-полей — это уже не стайлинг, а фича, отдельная задача.
- Проверки: `pnpm test` 339/339, computed-стили карточки сверены (radius 20px, shadow none→hover-shadow, transform translateY(-1px) на hover).

### Каталог: закругление крупных панелей, кнопка "Сбросить", :active на всех кнопках

- `.page-header`, `.catalog-filters`, `.catalog-sort-bar` уже имели правильный фон/бордер/стеклянную тень (из общего `:is(...)` списка), но радиус был крошечный (`--catalog-radius-md` = 7px, общий токен, трогать нельзя — завязан на 30 других мелких элементов) — точечно поднят до 24px, `.catalog-sort-bar` получил `overflow:hidden` (иначе прямоугольные сегменты сортировки торчали бы из скруглённых углов).
- `.catalog-filter-reset` ("Сбросить") не входил ни в один общий button-override — фон совпадал с фоном панели, кнопка была фактически невидима. Добавлен в общий secondary-button список (фон `#e3ded8`, radius 13px, hover).
- Бонус-баг от себя же (прошлый заход): `.catalog-view-toggle`/`.catalog-map-button` в базовых стилях — pill-кнопки (`border-radius:999px`), но мои общие secondary/primary-button правила задели их через `:is()` и придавили радиус до 13px. Восстановлено `999px !important` отдельным точечным правилом (тот же трюк со специфичностью `:is()`, что и раньше).
- Добавлено `:active` (нажатие) — раньше нигде не было `press`-эффекта, только hover. Теперь `translateY(1px)` на клике для всех кнопок (primary/secondary/фильтры/вид/карта/nav-item), `scale(0.94)` для круглых icon-кнопок сайдбара (toggle/theme) — как `.fw-btn:active{transform:translateY(1px)}` в рефе. Потребовалось добавить `transform` в `transition` у нескольких базовых правил (`.sidebar-toggle`, `.catalog-filter-toggle/-reset`), иначе press был бы мгновенным без анимации.
- Проверки: `pnpm test` 339/339, computed-стили сверены (radius 24px на панелях, 999px на pill-кнопках, hover/active состояния).

## 2026-09-13

### Перенос дизайна Fluffy White: сайдбар (светлая тема)

- Сайдбар перестроен по стайлгайду (fluffy-white-styleguide.../cabinet): плавающая «стеклянная» панель, свёрнут = видимый icon-rail 78px, развёрнут = 248px, бургер в шапке панели, иконки lucide у пунктов, снизу карточка пользователя (аватар/имя/роль) + переключатель темы + «Выйти». Контент/надписи платформы не менялись. Плюс тёплый светлый фон приложения (градиент #f8f7f6→#edeceb→#faf9f5 на #ece9e5) для `minimal-luxury`.
- Файлы: `apps/web/src/App.tsx` (разметка, иконки, `icon` в NavItem), `apps/web/src/styles.css` (структура панели, nav, фон), `apps/web/src/app-theme.css` (секция overrides для `html[data-app-theme="minimal-luxury"]`; удалено правило, прятавшее свёрнутый сайдбар). Тесты `sidebar-outside-click` и `app-theme` обновлены под новый контракт (rail вместо невидимого бургера).
- Важно: `platforma-web-1` в docker отдаёт старый билд из образа (без volume) — для проверки поднимался локальный vite на :5174 (API :3000, CORS обходили флагом браузера только в Playwright). Тёмная тема пока со старой стилистикой сайдбара — следующий этап.
- Проверки: web tests 339/339, `pnpm build:web` зелёный, UI-скриншоты (свёрнут/развёрнут/ховер/тёмная/мобайл) сверены локально.

### Пересборка web-контейнера + починка env ассистента

- `docker compose up -d --build web` пересобрал web (новый сайдбар на :5173, проверено логином в браузере), но recreate api уронил его: корневой `.env` имел старые `ASSISTANT_AI_MODE=openai` / `ASSISTANT_EMBEDDING_MODE=openai` / `EMBEDDING_MODEL=text-embedding-3-small`, а код после миграции принимает только `fake|alibaba`.
- Ключа Alibaba локально нет (`ALIBABA_API_KEY` пуст даже в `apps/api/.env`), поэтому в корневом `.env` оба режима выставлены в `fake`, модель оставлена `text-embedding-v4`, добавлены `ASSISTANT_ALIBABA_BASE_URL`/`STRUCTURED_OUTPUT`. Когда появится живой ключ — вернуть `alibaba` в обе переменные и прописать `ALIBABA_API_KEY` в корневой `.env` (compose читает только его).

### Миграция ассистента OpenAI → Alibaba DashScope (выполнено, локально, без коммита)

- Planner Luna/Terra → qwen-plus/qwen-max, embeddings → text-embedding-v4, весь транспорт — DashScope compatible-mode `/chat/completions` вместо Responses API. Env: `ALIBABA_API_KEY`, `ASSISTANT_ALIBABA_BASE_URL/TIMEOUT_MS/STRUCTURED_OUTPUT`, режим `ASSISTANT_AI_MODE=alibaba`; `OPENAI_API_KEY` остался только для Training. Цены в каталоге по официальному прайсу: qwen-plus $0.40/$1.20 (long >256K: $1.20/$3.60), qwen-max $1.60/$6.40, embedding $0.07 за 1M.
- Source discovery переведён на Chat Completions без web_search; live discovery заблокирован гейтом `ASSISTANT_SOURCE_DISCOVERY_WEB_SEARCH_UNVERIFIED`, обход — флаг `ASSISTANT_SOURCE_DISCOVERY_WEB_SEARCH_ACKNOWLEDGED=true` (только для stub-тестов и верификации). Промпты discovery всё ещё требуют веб-поиск — осознанно, до задачи по enable_search.
- Важно: usage-контракт нового транспорта — `cacheWriteInputTokens` и `webSearchCalls` всегда 0; eval/rollout/preflight ждут `providerMode`/`provider: 'alibaba'`. Production compose не получил `ASSISTANT_AI_MODE` (дефолт `:-fake` запрещён compose-контрактом) — включение ассистента на проде потребует явного добавления переменной.
- Проверки: `pnpm test` 960/960, `pnpm build`, assistant postgres-harnesses зелёные, t07 connected e2e зелёный (одно flaky-падение login при холодном web-контейнере, при повторе прошло). t06-postgres падает на свежей БД из-за отсутствия цепочки rollout events — pre-existing, подтверждено прогоном на до-миграционном коде. Не запускалось: t03-browser-runtime (нет привязки к LLM-транспорту), платные live smoke — ждут живого ключа Alibaba.

### Анализ замены OpenAI на Kimi/Moonshot (исследование, без изменений кода)

- OpenAI-ключ недействителен; оценили миграцию. SDK openai нет — везде raw fetch, но все 4 LLM-точки говорят на Responses API; у Kimi только Chat Completions (есть strict json_schema, tool calling, prompt caching). Нет у Moonshot: embeddings, Whisper, нативный web_search с allowed_domains.
- Минимум для planner: ~6 файлов, ~200–300 строк (gateway body/parser, имена моделей, pricing, гейт base URL). Полная миграция всех 6 точек: ~13 файлов, ~1.5–2k строк переписки.

### Сравнение китайских LLM-провайдеров для замены Luna/Terra (исследование)

- Кандидаты с OpenAI-совместимым Chat Completions: DeepSeek (V4-Flash $0.14/$0.28, V4-Pro ~$0.44/$0.87+ за 1M токенов), Qwen (3.7-Plus $0.40/$1.60, 3.7-Max $2.50/$7.50), GLM/Z.ai (4.7 $0.60/$2.20, 5.3 $1.40/$4.40), MiniMax (M2.7 $0.30/$1.20), Kimi (k2.6 $0.95/$4, k3 $3/$15).
- Strict json_schema из коробки подтверждён только у Kimi K3; DeepSeek — json_object-режим (нужна локальная валидация, у нас она есть). Рекомендация: Luna=DeepSeek V4-Flash, Terra=DeepSeek V4-Pro или Kimi k2.6. Embeddings/Whisper отдельно у всех китайцев.

## 2026-09-08

### Реконструкция статуса AI-ассистента (исследование, без изменений кода)

- Точка остановки: FIX-GEO2 реализован в коде полностью (коммиты 7f58604→e78a3c6, 06–07.09), все сценарии покрыты тестами; не закрыты две процедурные задачи — реальная загрузка справочника метро с полным покрытием опубликованных объектов (`assistant:metro:refresh --file`) и ручная UI-сверка времени с картой.
- До RC: все live-gates zaebal6 не пройдены (200-case eval, source/geo canary, adversarial UI, feed gate, rollout preflight) — каждый ждёт отдельной авторизации с платными вызовами. Rollout stage = ADMINS. Чекбоксы в zaebal1–6 не отмечены, хотя код этапов 1–6 на месте.
- Риск: FIX-GEO2 изменил planner-контракт после создания eval runner — перед RC-прогоном проверить, что frozen dataset и gates не инвалидированы.

### Создание журнала

- Заведён этот файл как shared-память между чатами внутри проекта.
- Источник требований всё равно остаётся: текущая задача → AGENTS.md → код.
- Правило: после завершения задачи агент дописывает сюда итоги.

---

## Шаблон записи

```markdown
### <YYYY-MM-DD> — <короткое название задачи>

<2–4 строки: что сделано, какие файлы тронуты, что важно помнить.>
```
